let str = "helllow world";

console.log(str);

class node {
  constructor(data, next = ull) {
    this.data = data;
    this, (next = next);
  }
}

class linkedList {
  constructor() {
    this.head = null;
    this.count = 0;
  }
}

// insertAt(index,data){
//     if(index > this.count || index <0){
//         throw new Error('범위를 넘어갔습니다.')
//     }
//     let newNode = new Node(data);
// };

// export {node};
